# Place trained model file here as: fake_review_model.pkl
# A scikit-learn pipeline (e.g. TfidfVectorizer + RandomForestClassifier)
# trained on at least 200 labeled PH-context samples.
# Label 1 = fake, Label 0 = genuine.
# The classifier must implement predict_proba on a list of raw strings.
